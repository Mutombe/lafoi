# La Foi backend — Render env vars
# -----------------------------------------------------------------------------
# Paste this whole file into Render's "Environment" tab via "Add from .env".
# Replace every <PLACEHOLDER> with a real value before saving.
# Mark SECRET_KEY and DATABASE_URL as Secret after import.
# -----------------------------------------------------------------------------

# --- Core Django ---
SECRET_KEY=<generate-a-fresh-50-char-random-string>
DEBUG=False
ALLOWED_HOSTS=<your-render-host>.onrender.com

# --- Database (Neon Postgres) ---
# Copy the full connection string from the Neon dashboard.
# Must include ?sslmode=require at the end.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require

# --- Frontend origin allow-lists ---
# Comma-separated, no trailing slashes. Include both bare and www if you use both.
CORS_ALLOWED_ORIGINS=https://lafoidesigns.co.zw,https://www.lafoidesigns.co.zw
CSRF_TRUSTED_ORIGINS=https://lafoidesigns.co.zw,https://www.lafoidesigns.co.zw

# --- Production security ---
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=31536000

# --- Media storage (Digital Ocean Spaces, S3-compatible) ---
# Required for media uploads (project photos, item images, payslip PDFs) to
# survive Render redeploys. Mark AWS_SECRET_ACCESS_KEY as Secret after import.
AWS_STORAGE_BUCKET_NAME=lafoi
AWS_S3_ENDPOINT_URL=https://sgp1.digitaloceanspaces.com
AWS_S3_REGION_NAME=sgp1
AWS_ACCESS_KEY_ID=<paste-DO-Spaces-access-key>
AWS_SECRET_ACCESS_KEY=<paste-DO-Spaces-secret-key>

# --- Company branding (used by PDF generators) ---
COMPANY_NAME=La Foi Designs
COMPANY_TAGLINE=Stretch Ceilings and Lighting
COMPANY_ADDRESS=Suite 26, 6 Chelmsford Rd, Belgravia, Harare
COMPANY_PHONE_PRIMARY=+263 782 931 472
COMPANY_PHONE_SECONDARY=+263 712 326 951
COMPANY_EMAIL=admin@lafoidesigns.co.zw
COMPANY_WEBSITE=https://lafoidesigns.co.zw
